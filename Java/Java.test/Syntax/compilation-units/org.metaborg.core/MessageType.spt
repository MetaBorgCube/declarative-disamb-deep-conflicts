module MessageType

language Java start symbol Start

test MessageType [[ package org.metaborg.core.messages;

public enum MessageType {
    PARSER, ANALYSIS, TRANSFORMATION, INTERNAL
} ]] parse succeeds
